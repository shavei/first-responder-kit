<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Deliberately minimal: Compose owns the real theming. This base theme only exists so
      the window background matches the app's dark surface, which removes the white flash
      during cold start (there is no splash screen).
    -->
    <style name="Theme.FirstResponderKit" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
