# The app uses no reflection, no serialization libraries and no JNI, so the default
# AGP/Compose/AndroidX consumer rules are sufficient. Keep line numbers so the rare
# crash report from a personal build is still readable.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
